import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;

module.exports = {
  images: {
    domains: ['localhost', 's2.loli.net', '1.z.wiki', 'cdn.z.wiki', '2.z.wiki'],
  },
}